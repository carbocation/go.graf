/data/bin/log/*.log {
	# Note that this only needs to be installed once per machine
	# It does not require per-project configuration
	
	size 50M
	rotate 10
	missingok
	notifempty
	copytruncate
	dateext
	compress
}